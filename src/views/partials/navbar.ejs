<nav class="navbar bg-white shadow-sm border-b z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Logo -->
            <div class="flex items-center">
                <a href="/" class="navbar-brand flex items-center">
                    <img src="https://res.cloudinary.com/dr5pehdsw/image/upload/v1769486903/channels4_profile_1.jpg_itbclm.jpg"
                        alt="Logo P4 Jakarta" class="h-10 mr-3 object-contain">
                    <span
                        class="font-heading font-bold text-base md:text-lg bg-gradient-to-r from-primary-700 via-primary-600 to-primary-500 bg-clip-text text-transparent">P4
                        Jakarta Timur</span>
                </a>
            </div>

            <!-- Navigation Links -->
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="navbar-link">Beranda</a>
                <a href="/about" class="navbar-link">Tentang</a>
                <a href="/artikel" class="navbar-link">Artikel</a>
                <a href="/programs" class="navbar-link">Pelatihan</a>
                <a href="/facilities" class="navbar-link">Fasilitas</a>
                <a href="/contact" class="navbar-link">Kontak</a>
            </div>

            <!-- Auth Buttons -->
            <div class="hidden md:flex items-center space-x-2 md:space-x-3">
                <% if (user) { %>
                    <!-- User Dropdown -->
                    <div class="relative" id="navUserDropdown">
                        <button type="button" id="navUserDropdownBtn"
                            class="flex items-center space-x-2 px-4 py-2 rounded-lg hover:bg-gray-100 transition-colors">
                            <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center">
                                <span class="text-white font-semibold text-sm">
                                    <%= user.nama ? user.nama.charAt(0).toUpperCase() : 'U' %>
                                </span>
                            </div>
                            <span class="hidden md:block font-medium text-gray-700">
                                <%= user.role==='admin' ? 'Super Admin' : user.nama %>
                            </span>
                            <svg class="w-4 h-4 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>

                        <!-- Dropdown Menu -->
                        <div id="navUserDropdownMenu"
                            class="hidden absolute right-0 mt-2 w-56 bg-white rounded-lg shadow-lg border border-gray-200 py-2 z-50">
                            <div class="px-4 py-2 border-b border-gray-100">
                                <p class="text-sm font-medium text-gray-800">
                                    <%= user.nama %>
                                </p>
                                <p class="text-xs text-gray-500">
                                    <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>
                                </p>
                            </div>
                            <a href="/<%= user.role %>/dashboard"
                                class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors">
                                <svg class="w-4 h-4 mr-3 text-gray-500" fill="none" stroke="currentColor"
                                    viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6">
                                    </path>
                                </svg>
                                Dashboard <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>
                            </a>
                            <a href="/<%= user.role %>/profile"
                                class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors">
                                <svg class="w-4 h-4 mr-3 text-gray-500" fill="none" stroke="currentColor"
                                    viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                                Profil Saya
                            </a>
                            <div class="border-t border-gray-100 my-1"></div>
                            <form action="/auth/logout" method="POST" class="block">
                                <button type="submit"
                                    class="flex items-center w-full px-4 py-2 text-sm text-red-600 hover:bg-red-50 transition-colors">
                                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1">
                                        </path>
                                    </svg>
                                    Logout
                                </button>
                            </form>
                        </div>
                    </div>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-outline">
                            <i class="fas fa-sign-in-alt mr-2"></i>
                            Login
                        </a>
                        <a href="/auth/register-peserta" class="btn btn-primary">
                            <i class="fas fa-user-plus mr-2"></i>
                            Daftar
                        </a>
                        <% } %>
            </div>

            <!-- Mobile Menu Button -->
            <button data-mobile-menu aria-expanded="false" aria-controls="mobile-nav" aria-label="Toggle navigation"
                class="md:hidden text-gray-700 hover:text-primary-600 focus:outline-none">
                <i class="fas fa-bars text-2xl"></i>
            </button>
        </div>
    </div>

    <!-- Mobile Menu (Hidden by default) -->
    <div id="mobile-nav" data-mobile-nav class="hidden md:hidden bg-white border-t border-gray-100 shadow-lg">
        <div class="px-4 pt-2 pb-4 space-y-1">
            <a href="/"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Beranda</a>
            <a href="/about"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Tentang</a>
            <a href="/artikel"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Artikel</a>
            <a href="/programs"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Program</a>
            <a href="/facilities"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Fasilitas</a>
            <a href="/contact"
                class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">Kontak</a>

            <div class="border-t border-gray-100 my-2"></div>

            <% if (user) { %>
                <div class="px-3 py-2">
                    <span class="block text-sm font-medium text-gray-500 mb-2">Login sebagai: <%= user.nama %></span>
                    <a href="/<%= user.role %>/dashboard"
                        class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">
                        <i class="fas fa-tachometer-alt mr-2"></i> Dashboard <%= user.role.charAt(0).toUpperCase() +
                            user.role.slice(1) %>
                    </a>
                    <a href="/<%= user.role %>/profile"
                        class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 hover:text-primary-600 hover:bg-gray-50">
                        <i class="fas fa-user mr-2"></i> Profil Saya
                    </a>
                    <form action="/auth/logout" method="POST" class="block">
                        <button type="submit"
                            class="w-full text-left px-3 py-2 rounded-md text-base font-medium text-red-600 hover:bg-red-50">
                            <i class="fas fa-sign-out-alt mr-2"></i> Logout
                        </button>
                    </form>
                </div>
                <% } else { %>
                    <div class="flex flex-col space-y-2 px-3 pt-2">
                        <a href="/auth/login"
                            class="w-full text-center px-4 py-2 border border-primary-600 text-primary-600 rounded-lg hover:bg-primary-50">
                            <i class="fas fa-sign-in-alt mr-2"></i> Login
                        </a>
                        <a href="/auth/register-peserta"
                            class="w-full text-center px-4 py-2 bg-primary-600 text-white rounded-lg hover:bg-primary-700 shadow-md">
                            <i class="fas fa-user-plus mr-2"></i> Daftar
                        </a>
                    </div>
                    <% } %>
        </div>
    </div>
</nav>