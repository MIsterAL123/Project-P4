<!-- Peserta Upload Surat Keterangan (disesuaikan dengan layout Surat Tugas Guru) -->
<div class="p-4 md:p-6">
  <!-- Page Header -->
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-800">Upload Surat Tugas</h1>
    <p class="text-gray-600 mt-1">Lengkapi pendaftaran dengan mengunggah Surat Tugas</p>
  </div>

  <div class="max-w-2xl mx-auto">
    <!-- Success Banner (show kuota info if available) -->
    <% if (typeof kuota !== 'undefined' && kuota) { %>
    <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
      <div class="flex items-start">
        <svg class="w-6 h-6 text-green-600 mr-3 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <div class="flex-1">
          <h3 class="text-lg font-medium text-green-800 mb-2">Pendaftaran Berhasil!</h3>
          <p class="text-green-700">Pendaftaran Anda untuk pelatihan "<%= kuota.judul_pelatihan || 'Pelatihan ' + kuota.tahun_ajaran %>" telah berhasil diajukan.</p>
        </div>
      </div>
    </div>
    <% } else if (typeof success !== 'undefined' && success) { %>
    <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
      <div class="flex items-start">
        <svg class="w-6 h-6 text-green-600 mr-3 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
        </svg>
        <div class="flex-1">
          <h3 class="text-lg font-medium text-green-800 mb-2">Pendaftaran Berhasil!</h3>
          <p class="text-green-700"><%= success %></p>
        </div>
      </div>
    </div>
    <% } %>

    <% if (typeof error !== 'undefined' && error) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
      <span class="block sm:inline"><%= error %></span>
    </div>
    <% } %>

    <!-- Upload Form Card -->
    <% if (!pendaftaran.surat_keterangan) { %>
    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h3 class="text-lg font-semibold text-gray-800 mb-4">Upload Surat Tugas</h3>
      
      <div class="bg-yellow-50 border border-yellow-200 rounded p-4 mb-6">
        <div class="flex">
          <svg class="w-5 h-5 text-yellow-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
          </svg>
          <div class="text-sm text-yellow-800">
            <p class="font-medium mb-2">Persyaratan Surat Tugas:</p>
            <ul class="list-disc list-inside space-y-1">
              <li>Ditandatangani oleh Kepala Sekolah/Pimpinan Instansi</li>
              <li>Mencantumkan nama lengkap dan jabatan</li>
              <li>Menyebutkan nama pelatihan yang akan diikuti</li>
              <li>Format: PDF, JPG, atau PNG (Maksimal 5MB)</li>
            </ul>
          </div>
        </div>
      </div>

      <form action="/peserta/upload-surat-keterangan/<%= pendaftaran.id %>" method="POST" enctype="multipart/form-data" id="uploadForm">
        <div class="mb-6">
          <label for="surat_keterangan" class="block text-sm font-medium text-gray-700 mb-2">File Surat Tugas <span class="text-red-500">*</span></label>
          <div class="mt-1 flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-lg hover:border-blue-400 transition-colors">
            <div class="space-y-1 text-center">
              <svg class="mx-auto h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
              <div class="flex text-sm text-gray-600">
                <label for="surat_keterangan" class="relative cursor-pointer bg-white rounded-md font-medium text-blue-600 hover:text-blue-500 focus-within:outline-none">
                  <span>Upload file</span>
                  <input id="surat_keterangan" name="surat_keterangan" type="file" class="sr-only" required accept=".pdf,.jpg,.jpeg,.png">
                </label>
                <p class="pl-1">atau drag and drop</p>
              </div>
              <p class="text-xs text-gray-500">PDF, JPG, PNG maksimal 5MB</p>
              <p id="fileName" class="text-sm text-blue-600 font-medium mt-2"></p>
            </div>
          </div>
        </div>

        <div class="flex flex-col sm:flex-row gap-3">
          <button type="submit" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center justify-center">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
            </svg>
            Upload Surat Tugas
          </button>
          <a href="/peserta/status-pendaftaran" class="px-6 py-2 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors text-center">Upload Nanti</a>
        </div>
      </form>
    </div>
    <% } else { %>
    <!-- Already Uploaded -->
    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h3 class="text-lg font-semibold text-gray-800 mb-4">Surat Tugas</h3>
      <div class="bg-green-50 border border-green-200 rounded p-4">
        <div class="flex items-center">
          <svg class="w-5 h-5 text-green-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="text-green-800">Surat Tugas sudah diunggah</span>
        </div>
      </div>
      <div class="mt-4">
        <%
          const suratKeteranganSlug = ((pendaftaran.judul_pelatihan || 'Surat Keterangan').toString()
            .replace(/[\/\\:*?"<>|]/g, '-')
            .replace(/\s+/g, '-')
            .replace(/[^a-zA-Z0-9._-]/g, '-')
            .replace(/-+/g, '-')
            .replace(/^-+|-+$/g, '') || 'surat-keterangan');
          const suratKeteranganExt = (pendaftaran.surat_keterangan && pendaftaran.surat_keterangan.includes('.'))
            ? pendaftaran.surat_keterangan.substring(pendaftaran.surat_keterangan.lastIndexOf('.'))
            : '.pdf';
          const suratKeteranganDocPath = encodeURIComponent(suratKeteranganSlug + suratKeteranganExt);
        %>
        <a href="/files/surat-keterangan/<%= pendaftaran.id %>/<%= suratKeteranganDocPath %>" target="_blank" class="text-blue-600 hover:text-blue-800 flex items-center">
          <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
          </svg>
          Lihat Surat Tugas
        </a>
      </div>
      <div class="mt-4">
        <a href="/peserta/status-pendaftaran" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors inline-block">Lihat Status Pendaftaran</a>
      </div>
    </div>
    <% } %>

    <!-- Info Card -->
    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
      <h3 class="text-lg font-semibold text-blue-800 mb-3">Langkah Selanjutnya</h3>
      <ol class="list-decimal list-inside space-y-2 text-blue-700">
        <li>Upload Surat Tugas yang telah ditandatangani</li>
        <li>Admin akan melakukan verifikasi dokumen</li>
        <li>Anda akan menerima notifikasi hasil verifikasi</li>
        <li>Jika disetujui, Anda dapat mengikuti pelatihan sesuai jadwal</li>
      </ol>
      <div class="mt-4 pt-4 border-t border-blue-200">
        <p class="text-sm text-blue-700">
          <strong>Catatan:</strong> Anda dapat mengunggah surat tugas nanti. Namun, pendaftaran Anda akan diproses setelah surat tugas diunggah dan diverifikasi.
        </p>
      </div>
    </div>
  </div>
</div>

<script>
(function initUploadSuratKeterangan() {
  const form = document.getElementById('uploadForm');
  const input = document.getElementById('surat_keterangan');
  const fileName = document.getElementById('fileName');
  const dropZone = document.querySelector('#uploadForm .border-dashed');
  const submitButton = form ? form.querySelector('button[type="submit"]') : null;

  if (!form || !input || !fileName) return;

  function updateFileName(inputElem) {
    if (inputElem.files && inputElem.files[0]) {
      fileName.textContent = 'File dipilih: ' + inputElem.files[0].name;
    } else {
      fileName.textContent = '';
    }
  }

  // Keep global for compatibility with existing inline usage patterns.
  window.updateFileName = updateFileName;

  input.addEventListener('input', function() {
    updateFileName(input);
  });

  input.addEventListener('change', function() {
    updateFileName(input);
  });

  form.addEventListener('submit', function(e) {
    if (!input.files || input.files.length === 0) {
      e.preventDefault();
      alert('Silakan pilih file surat tugas terlebih dahulu');
      input.click();
      return;
    }

    if (submitButton) {
      submitButton.disabled = true;
      submitButton.classList.add('opacity-75', 'cursor-not-allowed');
      submitButton.textContent = 'Mengirim...';
    }
  });

  if (!dropZone) return;

  dropZone.addEventListener('click', function(e) {
    if (e.target.closest('label') || e.target.closest('input[type="file"]')) return;
    input.click();
  });

  ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(function(eventName) {
    dropZone.addEventListener(eventName, function(e) {
      e.preventDefault();
      e.stopPropagation();
    }, false);
  });

  ['dragenter', 'dragover'].forEach(function(eventName) {
    dropZone.addEventListener(eventName, function() {
      dropZone.classList.add('border-blue-500', 'bg-blue-50');
    }, false);
  });

  ['dragleave', 'drop'].forEach(function(eventName) {
    dropZone.addEventListener(eventName, function() {
      dropZone.classList.remove('border-blue-500', 'bg-blue-50');
    }, false);
  });

  dropZone.addEventListener('drop', function(e) {
    const files = e.dataTransfer.files;
    input.files = files;
    updateFileName(input);
  }, false);
})();
</script>
